import React, { Component } from 'react'

/* 
admin的二级路由组件--角色（权限）路由组件
*/
export default class User extends Component {
    render() {
        return (
            <div>
                user
            </div>
        )
    }
}
